node_modules
.next
out
.env
.env.local
.env*.local
next-env.d.ts
.DS_Store

# VS Code workspace file (machine-specific, editor config only)
VEX.code-workspace

# Claude local settings (machine-specific, not for version control)
.claude/settings.local.json

# Build artifacts
tsconfig.tsbuildinfo

# Dev tooling
gstack/
.gstack/

# Supabase local state
supabase/.temp/
.vercel
.env.vercel.tmp

# Git worktrees (isolated workspaces for subagent-driven implementation)
.worktrees/

# AGENTS.md — cópia manual desatualizada do CLAUDE.md (find/replace quebrado,
# ver linha "Codex via Anthropic API"). Não versionar até existir processo
# real de sincronização — hoje viraria dívida de manutenção duplicada.
AGENTS.md
.env*
